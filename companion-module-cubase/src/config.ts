// Placeholder — replaced by Task 6 with real config fields (GetConfigFields, real MIDI port listing)
import type { JsonValue } from '@companion-module/base'

export interface ModuleConfig {
  [key: string]: JsonValue
}
